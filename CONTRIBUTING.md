# Como contribuir

Abra uma issue antes de mudanças grandes. Mantenha os Pull Requests pequenos, adicione testes e execute `npm run check` e `npm test`. Não inclua dados reais, segredos ou dependências sem justificativa.
